/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 */

import {StyleSheet, ViewStyle} from 'react-native';

export const styles = StyleSheet.create<{[k: string]: ViewStyle}>({
  drawerContainer: {
    height: '100%',
    backgroundColor: 'white',
    position: 'absolute',
  },
});
